name: panel
channels:
  - conda-forge
  - defaults
dependencies:
  - python
  - pandas
  - panel
  - holoviews
  - hvplot
  - jupyterlab
  - pytest
  - pip
# pip
  - pip:
      - mortgage
# install pyviz jupyterlab extension afterwards as well
